// import { PDFDocument, rgb, StandardFonts, degrees } from 'pdf-lib';
// // Take in a file, option to have metaData watermark and also physical watermark or both
// export async function addWatermark(file: File, mMark: boolean, pMark: boolean): Promise<void> {
//   // Convert the file into an ArrayBuffer, then turn it into pdf document
//   const arrayBuffer = await file.arrayBuffer();
//   const pdfDoc = await PDFDocument.load(arrayBuffer);

//   //add metaData mark
//   pdfDoc.
// }